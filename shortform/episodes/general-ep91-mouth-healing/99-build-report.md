# 91화 "혀 깨물었을 때 상처가 금방 낫는 이유" - 빌드 보고

## 산출물

- 배포 경로: `/home/lee/project/shorts/ko/[91화] 혀 깨물었을 때 상처가 금방 낫는 이유.mp4`
- 실측 재생시간: 42.944초 (ffprobe, 1287프레임 @ 30fps)
- 배포 시 md5 대조: `out/episode-ko.mp4`와 `shorts/ko/...` 파일의 md5가 동일함을 확인(a1b9cae337f1c306618a8e6b468e3f44)한 뒤 `out/`의 mp4를 삭제했다. `out/frames-ko/`, `out/stills/`는 검수 기록으로 남겨뒀다.
- 영어판은 요청받지 않아 만들지 않았다(원칙 6).

## 자산 재사용/신규

### 재사용 (REGISTRY 대조 완료)
- `character/Actor`, `character/BustActor`, `character/poses`(idle, touchNeck, surprised 계열 blend)
- `backgrounds/PlainBg`
- `scenes/CompareBars`, `scenes/Caption`, `scenes/Label`
- `props/ThemedIcon`(droplet, virus, x, check, dental 아이콘)
- `scenes/PulseRing`
- `audio/cold_zing.mp3`, `audio/realize_ding.mp3`, `audio/intro_ding.mp3`, `audio/outro_ding.mp3` (전부 기존 REGISTRY 7절 등록 SFX 재사용, 신규 합성 없음)

### 38화·54화 재사용 검토 결과 (대본 지정 검토 사항)
- `WoundCrossSectionDiagram`(general-ep38): 갓 베인 얕은 상처(딱지 커버리지 진행 중, 신경 노출)를 다룸. 이 화의 "이미 침 성분·혈관 밀도로 회복 속도가 갈린다"는 주제와 목적이 달라 재사용 불가로 판단.
- `WoundHealDiagram`(general-ep54): 딱지 앉은 상처가 "가려운" 이유(히스타민 방출·딱지 당김) 전용. 이 화는 가려움이 아니라 "회복 속도 비교"가 핵심이라 서사 구조가 달라 재사용 불가로 판단.
- 두 컴포넌트의 "원형 단면(피부 원 + 안쪽 옅은 조직 톤, CX/CY/R_OUTER/R_INNER)" 시각 문법과, `WoundHealDiagram`의 "입자 3개가 이동하는 물질" 언어는 그대로 계승해 새 컴포넌트에 반영했다.

### 신규 - `props/MouthHealDiagram.tsx` (1개)
- REGISTRY 등록 완료(추록 절, 본문 병합은 다음 정리 시 - 동시 편집 충돌 방지 규칙 준수)
- `assets/props/index.ts`에 export 추가 완료(기존 줄 유지, 파일 끝에 추가)
- 대본이 지정한 설계(독립 레이어 3종: `salivaProgress`/`vesselCompareProgress`/`deliverProgress`)를 그대로 구현
- s1(무성 - 씹다가 혀 깨물어 아파함 -> 하드 컷으로 며칠 뒤 다 나은 모습), s7(흉터 유무 비교 아이콘)은 에피소드 로컬 컴포넌트로, 신규 REGISTRY 등록 대상은 아님(재사용 가능성이 낮은 이 화 전용 비교 아이콘으로 판단)

## 언어별 실측 길이 (한국어만 - 영어판 미제작)

| 구간 | 내레이션 실측 길이 | 여백 |
|---|---|---|
| s1(무성, 고정) | 2.500s | - |
| s2(리액션, rate+32%/pitch+55Hz) | 5.568s | 0.6s(s2->s3 전환) |
| s3 | 4.584s | 0.2s |
| s4 | 4.488s | 0.2s |
| s5 | 4.992s | 0.2s |
| s6 | 4.608s | 0.2s |
| s7 | 2.592s | 0.2s |
| s8 | 4.632s | 0.2s |

본편 합계 35.8초 + Intro 2.3초 + TitleCard 1.8초 + Outro 3.0초 = 42.9초 (실측 42.944초)

## 렌더 횟수

- 한국어판 정식 렌더 1회 (스틸 선점검에서 결함을 미리 잡아 mp4 렌더는 1회로 끝남)
- 스틸(`remotion still`) 선점검: 1차 10장 + 결함 수정 후 재확인 7장 + 추가 스태거 재확인 3장 + 라벨 위치 재확인 1장 = 총 21장

## 기술 점검 항목과 결과 (관찰 기록)

- [x] **precheck.mjs**: 에러 0 / 경고 1(`SHAREDOUT` - 공용 루트 `out/`에 90화로 추정되는 frames-en/frames-ko 잔존, 이 화가 만든 것이 아니라 건드리지 않음). `KO-STR`·`RANDOM`·`NOWRAP`·`WORDBRK`·`FORMAT`·`IMPORT` 전부 무경고.
- [x] **자막이 화면 밖으로 나가지 않는가**: 최종 mp4 f001~f021(21개 대표 프레임) 전체 확인. 모든 캡션이 좌우 여백을 두고 화면 안에 들어옴(예: f16 "안이 피부보다 훨씬 빠르게 나아요" 좌우 여백 확인).
- [x] **장면 전환 시 캐릭터 잔상**: SceneSwitcher가 각 장면을 완전히 교체하는 방식이라 스틸/최종 프레임 모두에서 이전 장면 요소가 남아있는 것을 발견하지 못함.
- [x] **등장 전 요소가 점처럼 남아 있지 않은가**: MouthHealDiagram의 모든 레이어가 `progress > 0.01` 조건부 렌더(opacity 0이 아니라 아예 렌더 안 함)로 구현되어 있음을 코드로 확인. s3의 germ/입자, s5의 particle이 진행도 0에서 화면에 없음을 프레임(f010 등)에서 확인.
- [x] **라벨이 화면 밖에서 잘리지 않는가**: s4/s6/s7/s8 라벨 전부 화면 안. **s8 라벨이 캐릭터 안테나와 겹치는 결함을 스틸 선점검에서 발견**(아래 "발견한 결함" 절 참고) - 수정 후 f020에서 겹침 없음 재확인.
- [x] **요소끼리 겹치지 않는가**: s4 두 원이 의도적으로 살짝 맞닿는 디자인(20px 겹침, 두 부위가 이어진 하나의 "몸"이라는 인상을 주려는 의도) 외에는 겹침 없음. s1 체크 배지가 머리 윤곽선 가장자리에 걸치는 것은 "배지" 관례로 판단(하위 "관찰됨" 표시, 최종 판단은 사용자 몫).
- [x] **화면 아래쪽 여백이 과다하지 않은가**: **1차 스틸에서 s3(침방울 480px)가 화면 대비 지나치게 작아 상하좌우 여백이 과다한 결함을 발견**(아래 절 참고). MouthHealDiagram 내부 비율 확대(DROP_SIZE 260->460, R_OUTER 150->190) + 씬 크기 확대(S3 480->620, S4/S5 640->780)로 수정. 수정 후에도 diagram 하단부터 자막 사이에는 여전히 상당한 공백이 남아 있으나(예: s4 f012 원 하단 y≈865, 자막 상단 y≈1560, 약 700px 공백), 이는 WallVibrationDiagram(ep89)·CompareBars(ep87) 등 기존에 승인된 다른 화의 다이어그램 장면과 비슷한 수준의 공백으로 판단해 추가 수정하지 않음 - 확인 요청.
- [x] **음량이 충분한가**: `loudnorm=print_format=summary` 측정 결과 Input Integrated -13.7 LUFS, Input True Peak -2.2 dBTP. `volumedetect` 결과 mean -18.2dB / max -2.3dB(전체 mp4), 클리핑 없음.
- [x] 자막이 프로필 스타일(폰트·크기·위치)을 따르는가: `assets/theme.ts`/`assets/timeline.ts`의 공용 `Caption`/`CAPTION_STYLE`/`wrapCounts`를 그대로 사용, 별도 override 없음.
- 영어판 관련 항목: 해당 없음(영어판 미제작).
- [x] **캐릭터 윤곽선이 배경과 명확히 구분되는가**: 이 화의 모든 배경은 밝은 톤(`C.sky`/`C.room`/`C.paper`)이라 어두운 배경 대비 문제가 발생하는 장면이 없음(원칙 - `C.night` 계열 미사용).

### 프로필(general) 추가 체크
- 자막 한 줄 글자수 상한(한국어 20자)을 넘는 문장은 `buildCaptions`/`wrapCounts`가 자동으로 줄바꿈 처리, 개별 캡션 임의 조정 없음.
- 전문용어 노출: "점막"이라는 단어가 s4 라벨("입 안 점막")과 내레이션에 등장하나, 대본 원문에 있던 표현을 그대로 사용(건드리지 않음). 그 외 풀이 필요한 전문용어는 없음.

## 스틸 선점검에서 잡은 결함 (렌더 전, 코드만 수정하고 재확인 - mp4 렌더 없이 해결)

1. **s3(침방울) 화면 대비 과소 크기 + 화면 상하좌우 여백 과다**: 1차 스틸(`s3_saliva_peak.png`)에서 침방울이 480px 폭 안에서도 실제로는 지름 ~180px 수준으로 작게 그려져 화면 대부분이 빈 공간이었다(체크리스트 "화면 아래쪽 여백이 과다하지 않은가"). `MouthHealDiagram` 내부 침방울 크기를 260->460(viewBox 700 기준 약 60%로 확대, TearDropDiagram의 70% 채움 비율 참고)로 키우고, 씬 쪽 표시 크기도 480->620px로 키워 해결. 재확인 스틸(`s3_saliva_v2_peak.png`)에서 침방울과 세균+X 표시가 뚜렷하게 읽힘을 확인.
2. **s4/s5(단면 비교) 동일한 과소 크기 문제**: 1차 스틸(`s4_vessel_peak.png`)에서 원 두 개가 640px 폭 안에서 지름 ~300px로 작게 나왔다. `R_OUTER` 150->190, 씬 표시 크기 640->780px로 확대. 재확인 스틸(`s4_vessel_v2_peak.png`)에서 원 지름이 화면 대비 뚜렷하게 커짐을 확인.
3. **s5(입자 이동) 중간 진행도에서 입자 3개가 겹쳐 보임**: 1차 설계(스태거 간격 0.16, 구간 길이 0.5)로는 `deliverProgress=0.5`(중간 지점) 시점에 첫 입자가 이미 도착 완료 상태(t=1.0)라 다른 입자와 겹쳐 "몰려드는" 움직임이 잘 안 읽혔다(`s5_deliver_v2_mid.png`에서 발견). 스태거를 [0, 0.15, 0.3] + 구간 0.7로 넓혀 중간 지점에서도 3개 입자가 뚜렷이 다른 위치(도착 직전/중간/출발 직후)에 있도록 수정. 재확인 스틸(`s5_deliver_v3_mid.png`)에서 3개 입자가 서로 다른 위치에 분리되어 보임을 확인.
4. **s8 라벨("회복이 빨라요")이 캐릭터 안테나 머리카락과 겹침**: 1차 스틸(`s8_dental.png`)에서 `BUST_TOP + BUST_SIZE*0.08` 위치에 둔 라벨이 캐릭터 정수리의 안테나 형태 머리카락과 겹쳤다(21화 이후 결함 목록 B절 - "라벨이... 캐릭터 안테나...에 가려지거나 겹친다"). ep56의 `BUST_TOP - 90` 관례를 그대로 적용해 안테나 위로 옮겨 해결. 재확인 스틸(`s8_dental_v2.png`)에서 겹침 없음을 확인.

## 렌더 후(최종 mp4)에야 발견한 결함

없음. 스틸 선점검 단계에서 발견한 4건을 전부 코드 수정으로 해결한 뒤 최종 mp4를 렌더했고, 21개 대표 프레임 검수에서 추가로 발견된 결함은 없었다. 다만 아래는 "결함"으로 판정하지는 않았으나 관찰된 사항으로 기록한다.

- s1 healed 상태의 체크 배지가 캐릭터 머리 윤곽선 가장자리에 살짝 걸쳐 보인다(f005/f5_healed). 알림 배지가 아바타 가장자리에 걸치는 흔한 UI 관례와 유사한 형태로 판단해 결함으로 분류하지 않았으나, 확인을 요청한다.
- s4/s5의 두 단면 원이 서로 20px 겹치도록 의도적으로 배치했다(별개 부위가 아니라 "몸"으로 이어진 인상을 주려는 디자인 판단). 결함이 아니라 의도된 배치임을 기록해둔다.

## 오디오 SFX 배치 검증 (원칙 7)

- `cold_zing.mp3`(혀를 깨무는 순간): s1 로컬 프레임 28 = 절대 시각 5.033초. `silencedetect=noise=-40dB`로 최종 mp4를 실측한 결과 비무음 구간이 5.076~5.32초에서 관측되어 설계 지점과 약 0.04초 오차로 일치함을 확인.
- `realize_ding.mp3`(하드 컷 - 이미 나은 모습): s1 로컬 프레임 46 = 절대 시각 5.633초. 실측 비무음 구간 5.68~5.918초로 약 0.05초 오차로 일치함을 확인.
- 효과음 개별 피크가 내레이션 피크보다 낮은지: 원본 파일 실측(volumedetect) + Episode.tsx가 실제로 곱하는 volume 값 적용:
  - 내레이션(volume=1.6, +4.08dB): 원본 최대 -3.3dB(s2) ~ -4.6dB(s5) -> 적용 후 최대 약 0.78dBFS(s2)
  - cold_zing(volume=0.75, -2.5dB): 원본 -1.2dB -> 적용 후 약 -3.7dBFS
  - realize_ding(volume=0.85, -1.4dB): 원본 -4.2dB -> 적용 후 약 -5.6dBFS
  - 두 SFX 모두 내레이션 최대 피크보다 낮음을 확인.
  - 최종 믹스된 mp4에서도 직접 측정: SFX 구간(5.0~6.0s) max -6.6dB < 내레이션 구간(s3, 12.77~17.57s) max -2.3dB로 재확인.
- 인트로/아웃트로 딩 사운드는 기존 확정본(`intro_ding.mp3`/`outro_ding.mp3`)을 그대로 재사용했다(신규 제작 없음).

## 확인 요청

기술적 검증(precheck, 프레임 21장 육안 확인, 오디오 레벨·SFX 타이밍 실측)은 위와 같이 마쳤습니다. 특히 아래 2가지는 사용자 확인을 요청합니다.
1. s3~s5(MouthHealDiagram) 확대 수정 후에도 여전히 다이어그램 하단부터 자막 사이에 상당한 공백이 남아 있습니다(예: s4에서 원 하단~자막 사이 약 700px). 기존 다른 화 수준과 비슷하다고 판단해 추가로 손대지 않았는데, 이 정도면 괜찮은지 확인해주세요.
2. s1 체크 배지가 캐릭터 머리 윤곽선에 살짝 걸치는 부분(알림 배지 스타일로 의도)도 함께 확인해주세요.

이미 `shorts/ko/[91화] 혀 깨물었을 때 상처가 금방 낫는 이유.mp4`에 반영된 상태입니다.
