# 빌드 리포트 - 68화 (냉장고 안이 차가워지는 진짜 이유)

프로필: general. **한국어판만 제작** (영어 채널 Whymo 운영 중단, 2026-09-02).

## 자산

### 재사용 (REGISTRY 대조 완료)
- `backgrounds/PlainBg` (top=C.room, bottom=C.roomDeep - 주방 톤)
- `character/Actor` + `POSES.idle`/`POSES.surprised` (blendPose) - s1
- `props/ScentWaves` - s1 서늘한 바람, s7 손 접촉 온기 물결
- `scenes/Caption`의 `Label` - s2~s7 화면 라벨
- `assets/timeline.ts`(sceneFrames/sceneStarts/buildCaptions/wrapCounts/mouthAt/mouthProp), `anim.ts`(progress/blendPose/clamp01)
- `brand/Intro`, `brand/Outro`, `scenes/TitleCard`, `scenes/SceneSwitcher` (표준 조립)

### 신규 제작 -> REGISTRY 등록 완료
- `props/FridgeCycleDiagram.tsx` (`FridgeCycleDiagram`, `FRIDGE_VB_W`/`FRIDGE_VB_H`/`FRIDGE_BACK_PT`)
  - 사전 확인: `CounterCurrentDiagram`(ep52, 대향류 열교환)과 `MicrowaveDiagram`/`PlateFoodIcon`(ep31, 가전+온도 변화) 검토. 둘 다 "닫힌 루프를 돌며 팽창-압축 단계를 거치는 열펌프 순환" 구조는 다루지 않아 신규 제작.
  - 냉장고를 사실적으로 그리지 않고 사각 상자 하나를 세로 분할선으로 안(선반+음식)/뒤(방열판+압축기)로만 구분. 냉매 파이프는 굵은 선 하나의 닫힌 루프(증발기 지그재그 -> 압축기 원 -> 응축기 지그재그 -> 상단 복귀).
  - 독립 progress 6개: `revealProgress`/`expandProgress`/`absorbProgress`/`compressProgress`/`releaseProgress`/`loopProgress`.
  - 다른 화(에어컨·히트펌프 등)에서 재사용 가능성 있어 에피소드 로컬이 아니라 라이브러리에 등록.

## 실측 타임코드 (한국어, 언어별 실측)

- s1~s7 실측 발화 길이: 3.576 / 5.472 / 5.760 / 5.448 / 5.520 / 5.400 / 6.288초 (전 구간 여백 0.2초)
- 본편(s1~s7) 합계: 1166프레임 = 38.87초
- 전체(인트로+제목카드+본편+아웃트로): 1379프레임 = 46.02초 (60초 상한 대비 여유, 늘리지 않음)
- 상세는 `02-script-final-ko.md` 참고

## 기술 점검

### precheck.mjs
- 1차 실행: 에러 0 / 경고 1(`SHAREDOUT` - 공용 루트 `out/`에 다른 화(67화로 추정)의 `frames-ko/`·`frames-en/`가 있음. 병렬 렌더 중인 다른 화 산출물로 판단해 손대지 않음)
- 최종(FridgeCycleDiagram 좌표 수정 후) 재실행: 동일하게 에러 0 / 경고 1(SHAREDOUT, 오탐 아님 - 여전히 다른 화 소유로 판단, ep68 산출물 아님)

### 렌더 전 스틸 선점검 (원칙 준수, 렌더 1회로 마무리)
- 1차 스틸 7장(s1 mid, s2 reveal 끝, s3~s6 각 progress peak, s7 loop+hand)에서 **결함 2건 발견 후 렌더 전 수정**:
  1. **s4 열 흡수 화살표가 사과 소품과 겹침** - 화살표 시작 좌표를 사과(150,252,r26)에서 충분히 떨어지도록 이동(`props/FridgeCycleDiagram.tsx`의 `absorbArrows` 좌표 수정)
  2. **s7 "손을 대는 동작" 흰 타원이 압축 온도계 스템과 겹침** - `FRIDGE_BACK_PT`를 응축기 근처(x655,y95)에서 뒤쪽 영역 중간 높이(x650,y380)로 이동해 온도계·응축기 지그재그와 겹치지 않게 함. 아울러 `<g opacity>`를 SVG 컨텍스트 밖에서 쓰던 것(비표준)을 `<div style={{opacity}}>`로 교체
  - 부수적으로 s6 열 방출 물결의 중심좌표가 실제 응축기 위치(y~90)가 아니라 y=250에 잘못 고정돼 있던 것을 발견해 함께 수정(응축기와 정렬), 응축기 지그재그 자체도 진폭을 넓혀 시각적 비중을 증발기와 맞춤
  - **첫 스틸 배치에서 "박스가 안 보인다"고 오판했다가 디버그 텍스트로 재확인한 결과 실제로는 박스가 정상 렌더되고 있었음(착시/육안 오독)** - 코드 결함이 아니었음을 픽셀 좌표 실측(PIL)으로 확인 후 디버그 코드 제거하고 계속 진행
- 수정 후 스틸 재확인 6장에서 위 2건 모두 해소 확인, 추가 결함 관찰되지 않음

### 렌더 후 mp4 검수 (episode-ko.mp4, 1379프레임/46.02초)
- ffprobe: 1080x1920, 30fps, 1379프레임 확인(예상과 정확히 일치)
- 대표 프레임 20장 추출(인트로 mid, 제목카드 mid, s1~s7 시작/중간/경계, 아웃트로 mid) 후 Read로 육안 확인
- 자막 화면이탈: 20개 프레임 전체에서 자막 박스가 좌우 여백을 두고 화면 안에 들어옴 확인 (s1의 긴 문장, s7의 긴 문장 포함)
- 장면 전환 시 캐릭터 잔상: s1(캐릭터)->s2(다이어그램) 전환 프레임(f238, 전역 프레임 238)에서 SceneSwitcher 표준 크로스페이드(xfade=6, fadeIn=8)로 인한 일시적 저채도 겹침 확인 - 이는 공용 SceneSwitcher의 설계된 동작(모든 화 공통)이며 새로운 결함이 아님. 캐릭터가 다음 장면에 "얼어붙은 채" 남는 등의 실제 잔상은 관찰되지 않음
- 등장 전 요소 잔상(점): opacity 기반 리빌만 사용(scale 0 미사용)이라 사전 등장 흔적 관찰되지 않음
- 라벨 화면이탈: "냉매"/"팽창 -> 온도 뚝"/"열 흡수"/"압축 -> 온도 확"/"열 방출"/"열을 계속 밖으로 퍼내는 중" 6개 라벨 전부 화면 중앙 기준 좌우 여백 안에서 확인
- 요소 겹침: 위 스틸 선점검에서 발견한 2건 수정 반영 확인, 최종 mp4에서 재발 없음
- 화면 하단 여백: s2~s7에서 다이어그램 하단(화면 y≈1159)과 자막 박스 상단(화면 y≈1478) 사이에 약 320px 빈 공간 관찰됨 - ep31/ep52 등 기존에 배포된 다이어그램 중심 화들과 동일한 레이아웃 관례(다이어그램 y=380~440 시작)를 따른 것으로, 이번 화에서 새로 발생한 문제는 아님. 과다 여부는 확인만 하고 판단은 사용자에게 맡김
- 음량: `loudnorm` 측정 결과 Input Integrated -13.3 LUFS / Input True Peak -2.4 dBTP - 이전 화들과 비슷한 수준으로 관찰됨
- 자막 스타일: `assets/theme.ts` `CAPTION_STYLE`/프로필 설정(폭 20자, 폰트 50px, 화면 하단 23% 지점) 그대로 사용, 별도 오버라이드 없음
- 캐릭터 윤곽선 대비: 배경이 밝은 톤(C.room/C.roomDeep, 크림 계열)이라 `C.ink` 기본 스트로크로 충분히 구분됨 확인(어두운 배경 아님, 글로우 처리 불필요)
- 프로필 추가 체크(general.md 8절): 소재가 "냉장고 열면 서늘한 바람" 일상 경험 기반 궁금증에 해당, 어미가 "~거든요/~예요" 대화체 유지, "냉매"·"압축기" 2개 전문용어가 각각 등장 즉시 풀이됨("특수한 액체", "지나며 확 압축되는데"로 문맥상 풀이), 자막 한 줄 20자 이내 확인, 60초 상한 이내(46.02초) 확인

## 렌더 횟수

- 한국어: 1회 (스틸 선점검에서 결함을 미리 잡아 렌더 1회로 마무리)
- 영어: 미제작 (원칙 6에 따라 요청받지 않아 제작하지 않음)

## 배포

- 배포 경로: `/home/lee/project/shorts/ko/[68화] 냉장고 안이 차가워지는 진짜 이유.mp4`
- md5 대조: `out/episode-ko.mp4`와 배포본 일치 확인 후 `out/`의 mp4 삭제(`out/frames-ko/`, `out/stills/`는 보존)
- 영어판 배포 없음(미제작)

## 확인 요청

위 관찰 내용을 바탕으로 배포된 mp4를 직접 확인해 주세요. 특히 s2~s7 다이어그램 하단 여백 정도, 자막 타이밍 체감, 순환 다이어그램의 "차갑게 만드는 게 아니라 열을 밖으로 퍼낸다"는 반전이 s6~s7에서 명확히 읽히는지를 봐 주시면 좋겠습니다.
