프로필: general
추정 길이: 한국어 약 52.4초 / 영어 약 50초 (낭독 기준 추정 - s1 무성 2.0초 포함, 문장부호 무음 포함. 확정은 builder의 언어별 TTS 실측. 60초 상한 안에서 늘어난 만큼만 썼고, 채우기 위해 억지로 늘리지 않았다 - 원칙 0)
사실 개수: 1개(사슬 2단, s3~s5 - v1의 두 핵심 사실은 그대로이고 "계면활성제"·"미셀"이라는 정확한 이름만 추가했다) + 곁가지 3개(s6 손 씻을 때 20초 문지르라는 권장의 이유 - 확신 있는 실용 사실 / s7 코로나 바이러스 등 지질막 바이러스에 비누가 통하는 원리 - 확신 있는 응용 사실 / s8 비누의 역사 - 확신 있는 역사 사실, 정확한 연도는 정성적으로 처리)

## v1 -> v2 변경 사유

사용자 피드백 반영(재작성 모드).

1. **길이 확장**: "대본이 너무 짧지 않나" 지적을 반영해 27초(문장 5개)에서 52.4초(문장 9개)로 늘렸다. s6(손 씻기 20초 권장의 이유)·s7(바이러스에도 같은 원리가 통하는 것)·s8(비누의 역사)을 새로 추가했다. **핵심 두 사실(물과 기름이 왜 안 섞이는지 - 비누가 어떻게 해결하는지)은 바뀌지 않았다.**
2. **용어**: 7화(횡격막)·4화(산소→공기 순화) 사례처럼 "이름을 밝히라"는 지적을 이번 화에 적용했다. v1은 "비누 분자는 한쪽은 물을, 반대쪽은 기름을 좋아하는 특이한 모양"이라고만 하고 이 분자의 이름과, 기름을 감싼 덩어리의 이름을 끝까지 밝히지 않았다. v2에서는 s4에서 "계면활성제"(비누·샴푸·세제에 공통으로 들어있는 분자의 정식 이름), s5에서 "미셀"(감싸인 덩어리의 이름)을 각각 밝히고, 그 자리에서 바로 쉬운 말로 풀었다(계면활성제는 s4 앞 문장에서 이미 설명한 "양쪽 성질이 다른 특이한 모양"을 그대로 가리키고, 미셀은 "작고 동그란 덩어리"라는 설명이 이름 바로 앞에 붙는다). 둘 다 프로필의 "한 편에 용어 2개까지" 한도 안이다(원칙 3).

## 곁가지 확인 (v2 추가분, 원칙 1)

| # | 내용 | 확신 | 처리 |
|---|---|---|---|
| s6 | 손을 씻을 때 최소 20초 문지르라고 권장되는 이유는, 비누가 세균을 감싸서 피부에서 떼어내는 데 그 정도 접촉·마찰 시간이 필요하기 때문이다 | 핵심 사실(확신 있는 보건 상식, WHO·CDC 등에서 널리 안내하는 권장 근거) | 정확한 세균 제거율 수치는 넣지 않고 "그 정도 시간이 걸린다"는 정성적 표현만 사용 |
| s7 | 코로나 바이러스 등 일부 바이러스는 겉면이 기름(지질) 성분 막으로 덮여 있어, 비누 분자가 그 막을 파괴해 바이러스를 무력화한다 | 핵심 사실(확신 있는 응용 화학·바이러스학, 코로나19 시기 널리 알려진 설명) | 모든 바이러스가 아니라 "지질막이 있는 바이러스"에 한정된다는 점을 "코로나 바이러스처럼"으로 한정해 과도한 일반화를 피함 |
| s8 | 비누는 아주 오래전(고대)부터 있었고, 기름과 재(알칼리 성분)를 섞은 것이 초기 형태였다 | 핵심 사실(확신 있는 역사, 다만 정확한 연도·최초 발명 문명은 기억이 갈림) | 정확한 연도·지역명은 넣지 않고 "아주 오래전"으로 정성적으로 처리 |

## 제목 (제목 카드용, 원칙 6) - 재검토, 변경 없음

- KO: `비누가 기름을 씻어내는 이유`
- EN: `Why Soap Can Wash Away Oil (But Water Can't)`

재검토 결과: 새로 추가된 곁가지(손 씻기 20초, 바이러스, 비누의 역사)는 전부 "비누가 왜 기름때를 씻는가"라는 같은 질문에 대한 보강일 뿐 질문 자체를 바꾸지 않는다. 제목은 v1 그대로 유지한다. 후보 검토·선정 이유는 v1과 동일(v1 파일 참고).

## 해시태그 (업로드 메타데이터용, 원칙 6) - 재검토, 1개 추가

- KO: `#굼구미 #상식 #비누 #화학 #계면활성제 #생활과학 #쇼츠`
- EN: `#Whymo #DidYouKnow #Soap #Chemistry #Surfactant #Science #shorts`

변경: `#계면활성제`/`#Surfactant`를 추가했다. s4에서 실제로 이 용어를 명시적으로 다루므로 대본 사실 범위 안의 추가다. 나머지 태그는 v1과 동일.

## 설명 (업로드 캡션용, 원칙 6) - 재검토, 1문장 추가

- KO: `물로만 헹궈서는 기름기가 잘 없어지지 않죠. 비누 분자(계면활성제)는 한쪽은 물을, 반대쪽은 기름을 좋아하는 특이한 모양이라 기름방울을 감싸서 물속에 띄워버립니다. 참고로 손을 씻을 때 20초는 문질러야 한다는 것도, 비누가 세균을 감싸서 떼어내는 데 그만큼 시간이 걸리기 때문입니다.`
- EN: `Rinsing with plain water barely removes grease. Soap molecules, called surfactants, have a strange shape - one end loves water, the other loves oil - so they wrap around grease droplets and float them into the water. That's also why you're told to scrub your hands for 20 seconds: it takes soap that long to wrap around and lift off germs.`

구성 메모: 1·2문장은 v1과 거의 동일(2문장에 "계면활성제"라는 용어만 괄호로 추가). 3문장을 새로 넣어 s6(손 씻기 20초)의 곁가지를 캡션 안에서 짧게 완결했다. s7(바이러스)·s8(역사)은 캡션에 넣지 않았다 - 원칙 6 설명 절 1번("1~3문장, 짧게")을 지키기 위해 가장 생활 밀착형인 s6만 반영했다. 구독·좋아요 유도 문구는 넣지 않았다.

## 장면 (언어 공용)

**구성 변경**: s1~s3(오프닝~물기름 분리)은 v1과 동일하다. v1의 s4(비누 분자 구조)·s5(감싸서 띄움)에 용어("계면활성제"·"미셀")만 추가했다. s6~s8이 신규 곁가지다.

| # | 구간(추정, KO 기준) | 장면 지시 | 화면이 담당 | 목소리가 담당 | 화면 문자(언어별) |
|---|---|---|---|---|---|
| s1 | 0:00-2.00 | **[v1과 동일]** 캐릭터가 기름 묻은 접시를 물로만 헹구는데 기름 얼룩이 그대로 남아있다(`SoapMicelleDiagram` 정지 상태) | 접시 위 기름 얼룩이 안 지워지는 모습 | (없음 - 무성) | (없음) |
| s2 | 2.00-6.50 | **[v1과 동일]** 캐릭터가 접시를 보며 갸웃한다(`shrug` 포즈) | 캐릭터 리액션 표정 | "어? 물로만 헹구는데 기름이 계속 남아있네. 왜 이러지?" | (없음) |
| s3 | 6.50-11.50 | **[v1과 동일]** 물방울과 기름방울이 서로 밀어내며 절대 안 섞이는 모습(`SoapMicelleDiagram separateProgress`) | 물방울·기름방울이 분리되어 있는 모습 자체 | "물이랑 기름은 원래 성질이 정반대라서, 그냥 물만으로는 절대 안 섞이거든요." | (없음) |
| s4 | 11.50-21.00 | **[v2 확장]** 막대 모양 비누 분자들이 등장해 기름 방울 주위를 하나씩 둘러싼다(`SoapMicelleDiagram surroundProgress`) - 후반부에 화면 상단에 "계면활성제" 라벨이 팝인 | 비누 분자가 기름방울을 감싸는 과정 + "계면활성제"라는 이름(원칙 2 - 이름은 화면이 전담) | "근데 비누 분자는 한쪽은 물을 좋아하고, 반대쪽은 기름을 좋아하는 특이한 모양이에요. 이런 분자를 계면활성제라고 해요." | ko `물을 좋아함` / `기름을 좋아함` → `계면활성제` · en `Loves Water` / `Loves Oil` → `Surfactant` |
| s5 | 21.00-31.10 | **[v2 확장]** 감싸인 기름방울 덩어리가 물살에 떠내려가고 접시가 깨끗해진다(`SoapMicelleDiagram floatAwayProgress`) - 덩어리 옆에 "미셀" 라벨이 함께 붙는다 | 기름 덩어리가 물에 떠서 씻겨 나가는 모습 + "미셀"이라는 이름 | "그래서 기름방울을 감싸서 작고 동그란 덩어리로 만들어요. 이 덩어리가 미셀인데, 물속에 떠서 헹굴 때 같이 씻겨 나가요." | ko `미셀` · en `Micelle` |
| s6 | 31.10-39.80 | **[v2 신규]** 화면 전환. 캐릭터 손이 거품과 함께 씻기고, 옆에 `CountUp`이 0에서 20까지 올라간다(단위 "초") | "20초" 카운트업(원칙 2) | "그래서 손을 씻을 때 적어도 20초는 문질러야 한다고 하는데, 비누가 세균을 감싸서 떼어내는 데 그 정도 시간이 걸리기 때문이에요." | ko `20초` · en `20 seconds` |
| s7 | 39.80-45.10 | **[v2 신규]** 바이러스 모양 아이콘(`ThemedIcon virus`) 주위에 얇은 막 표시(`PulseRing`)가 둘러져 있다가, 비누 분자가 다가와 닿는 순간 막이 `FlashOverlay`로 터진다 | 바이러스를 감싼 막이 비누에 닿아 터지는 순간 | "코로나 바이러스처럼 겉면이 기름막인 바이러스는, 비누가 그 막을 터뜨려서 무력화시켜요." | ko `기름막` → `무력화` · en `Fatty Layer` → `Destroyed` |
| s8 | 45.10-52.40 | **[v2 신규]** `Card` 팝인(배지 `아주 오래전`, 라벨 `기름 + 재 = 비누의 시작`, art `ThemedIcon flask`) | "아주 오래전" 배지 + "기름 + 재" 라벨(원칙 2 - 정확한 연도는 확신이 없어 화면에도 안 넣음) | "참고로 비누는 아주 오래전부터 있었어요. 기름과 재를 섞어서 쓴 게 비누의 시작이었대요." | ko `아주 오래전` / `기름 + 재 = 비누의 시작` · en `Ancient Times` / `Fat + Ash = First Soap` |

s4·s5는 v1의 화면·전개를 그대로 쓰되 용어 라벨만 후반부에 덧붙였다 - 내레이션이 이미 설명한 내용을 화면이 이름으로 확정해주는 구조라 중복이 아니다(원칙 2). s6~s8은 순서대로 "실용(손 씻기)→응용(바이러스)→역사(기원)"로 서로 다른 각도의 보너스라 부연이 아니다(원칙 1-1 판별 기준 통과).

## 내레이션 대조표

| # | 한국어 | English |
|---|---|---|
| s1 | (없음) | (none) |
| s2 | 어? 물로만 헹구는데 기름이 계속 남아있네. 왜 이러지? | Huh, I'm rinsing with just water, but the oil's still there. Why won't it go? |
| s3 | 물이랑 기름은 원래 성질이 정반대라서, 그냥 물만으로는 절대 안 섞이거든요. | Water and oil are just opposites by nature, so plain water alone can never mix with it. |
| s4 | 근데 비누 분자는 한쪽은 물을 좋아하고, 반대쪽은 기름을 좋아하는 특이한 모양이에요. 이런 분자를 계면활성제라고 해요. | But soap molecules are what's called surfactants - one end loves water, the other end loves oil. |
| s5 | 그래서 기름방울을 감싸서 작고 동그란 덩어리로 만들어요. 이 덩어리가 미셀인데, 물속에 떠서 헹굴 때 같이 씻겨 나가요. | So it wraps around the grease droplet, forming a tiny ball called a micelle. That micelle floats in the water, so it rinses away with everything else. |
| s6 | 그래서 손을 씻을 때 적어도 20초는 문질러야 한다고 하는데, 비누가 세균을 감싸서 떼어내는 데 그 정도 시간이 걸리기 때문이에요. | That's also why you're supposed to scrub your hands for at least 20 seconds - it takes soap that long to wrap around germs and lift them off your skin. |
| s7 | 코로나 바이러스처럼 겉면이 기름막인 바이러스는, 비누가 그 막을 터뜨려서 무력화시켜요. | Viruses like coronavirus have a fatty outer layer, so soap can literally burst that layer and disable them. |
| s8 | 참고로 비누는 아주 오래전부터 있었어요. 기름과 재를 섞어서 쓴 게 비누의 시작이었대요. | By the way, soap has been around for a very long time. The earliest versions were just fat mixed with ash. |

## 영어판 의역 메모

- (v1과 동일, s2·s3) 문장을 바꾸지 않았으므로 기존 의역 메모 그대로 유지
- s4 (v2 용어 추가): 영어 관습대로 이름을 먼저 던지고 정의를 뒤에 붙였다 - `"soap molecules are what's called surfactants - one end loves water, the other end loves oil"`(이름 → 설명 순서, 원칙 4). 한국어는 반대로 설명을 먼저 하고 이름을 뒤에 붙였는데(한국어 관습), 두 언어 모두 "설명과 이름이 한 문장 안에 함께 있다"는 원칙은 지켰다
- s5 (v2 용어 추가): `"forming a tiny ball called a micelle"`으로 이름이 등장하는 자리에서 바로 "a tiny ball"이라는 쉬운 말이 붙는다. 화면 라벨("Micelle")을 다시 읽지 않고 "That micelle floats ~"로 이어받았다(원칙 2)
- s6 (v2 신규): "적어도 20초"를 화면(CountUp)이 숫자로 보여주므로 내레이션에서는 "for at least 20 seconds"라고 자연스럽게만 언급하고 숫자를 반복 강조하지 않았다
- s7 (v2 신규): "코로나 바이러스처럼"의 한정 표현을 `"Viruses like coronavirus"`로 그대로 살려 "모든 바이러스"로 과잉 일반화되지 않게 했다(원칙 1)
- s8 (v2 신규): "아주 오래전부터"를 `"a very long time"`으로, 정확한 연도를 말하지 않는 정성적 처리를 영어에서도 동일하게 유지했다

## 자산 목록

### 기존 라이브러리 재사용

- (v1과 동일) `backgrounds/PlainBg`, `character/Actor`+`poses`(`shrug`), `scenes/Caption`, `scenes/Label`, `scenes/Effects`(`Appear`), `scenes/TitleCard`/`brand/Intro`/`brand/Outro`
- (v1과 동일, s1·s3·s4·s5에서 계속 재사용) `props/SoapMicelleDiagram`
- (v2 신규 재사용) `scenes/Counter`의 `CountUp` - s6 "20초" 카운트업
- (v2 신규 재사용) `props/ThemedIcon`의 `virus`(s7) `flask`(s8) - 렌더 전 캐시에 없으면 `node scripts/sync_icons.mjs virus flask` 동기화 필요(REGISTRY 규칙)
- (v2 신규 재사용) `scenes/Effects`의 `PulseRing`(s7 바이러스 막 표시) `FlashOverlay`(s7 막이 터지는 임팩트)
- (v2 신규 재사용) `scenes/Card` - s8 "아주 오래전 / 기름+재" 카드(ep06 등에서 이미 쓰인 패턴 재사용)

### 새로 만들어야 함

- (v1과 동일) 없음. v2에서도 새 컴포넌트를 만들지 않고 전부 기존 라이브러리(`SoapMicelleDiagram`, `Card`, `CountUp`, `ThemedIcon`, `PulseRing`, `FlashOverlay`)를 조합해 처리했다
