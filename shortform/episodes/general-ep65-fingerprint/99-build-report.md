# 65화 "쌍둥이도 지문이 다른 이유" - 빌드 보고

**한국어판 1개만 제작했다** (오케스트레이터 명시 지시 - 영어 채널 Whymo 운영 중단, 2026-09-02).

## 1. 자산

### 재사용 (신규 제작 없이 그대로 가져다 씀)
- `character/BustActor`, `POSES.idle`/`POSES.thinking` - s2 리액션, s6 쌍둥이 캐릭터(같은 얼굴 2개 나란히, `blinkOffset`만 다르게 줘 눈깜빡임 위상을 어긋냄)
- `props/Finger`(`props/Hand.tsx`) - s5 자궁 속 손끝 클로즈업(기존 손가락 소품 그대로 재사용, `wrinkle=0`)
- `backgrounds/PlainBg` - 전 장면 공용 배경 (s5는 `top`/`bottom`/`stop`만 오버라이드해 옅은 물빛 톤으로)
- `scenes/Card`, `scenes/CardGrid`, `scenes/Label`, `scenes/Caption` - s1 잉크 카드, s3 라벨, s5 라벨, s6 쌍둥이 지문 비교 카드
- `assets/audio/ink_splat.mp3` - REGISTRY에 "액체가 얼굴/사물에 정통으로 맞는 임팩트 전반 재사용 가능"으로 등록된 효과음을 손끝이 종이를 눌러 지문을 남기는 순간에 재사용(신규 SFX 제작 없음)
- `assets/audio/intro_ding.mp3`, `outro_ding.mp3` - 브랜드 범퍼 사운드
- `assets/brand/Intro`, `assets/brand/Outro`, `assets/scenes/TitleCard`

### 새로 제작 (REGISTRY 등록 완료)
- `assets/props/FingerprintFormationDiagram.tsx` - `FingerprintFormationDiagram`(손끝 피부 단면, 표피/진피 경계가 `growProgress`(0~1)로 평평한 직선 -> 물결치는 굴곡 곡선으로 자리 잡음)과 `FingerprintSwirl`(완성된 지문 무늬 - 동심 루프 5겹, `variantSeed`로 결정적 미세 변주) 두 컴포넌트를 한 파일에 묶었다.
- REGISTRY 사전 조회: `props/Hand.tsx`의 `FingerCrossSection`(혈관 좁아짐), `props/FingertipNerveDiagram.tsx`(신경 다발, 38화)를 먼저 확인했으나 둘 다 손가락 단면·클로즈업을 다룰 뿐 "두 피부층이 다른 속도로 자라며 경계가 굴곡진 패턴으로 자리 잡는" 발생 과정도, "완성된 지문 무늬 자체"도 다루지 않아 재사용이 아니라 신규 제작으로 판단했다.
- `assets/props/index.ts`, `assets/REGISTRY.md`(262행 신규 삽입) 둘 다 **기존 줄은 건드리지 않고 끝에/해당 위치에 추가만** 했다(64화 동시 작업 주의사항 준수).

## 2. 실측 타임코드 (한국어)

내레이션 실측 합계 34.68초, 본편(s1~s7) 1172프레임(39.067초), 인트로+제목카드+본편+아웃트로
합계 **1385프레임 = 46.1667초**(30fps 계산값, ffprobe 컨테이너 길이 46.229333초 - 오디오
트레일링 패딩으로 인한 미세한 차이, 영상 프레임 수는 1385로 정확히 일치). 상세는
`02-script-final-ko.md` 참고.

TTS: `ko-KR-SunHiNeural`, rate +20%, pitch +30Hz (프로필 general 기본값). s2(리액션+훅 질문)도
같은 기본값을 그대로 썼다 - "이 무늬, 왜 사람마다 다 다르게 생긴 거지?"는 놀람·통증보다는
가벼운 의문에 가까워 별도 리액션 톤 오버라이드를 하지 않았다(61화와 동일 판단 근거).

## 3. 렌더

- precheck.mjs: 에러 0 / 경고 1(`SHAREDOUT` - 공용 루트 `out/`에 남아있는 스테일 프레임 폴더를
  가리킴, 이번 화 산출물이 아니므로 소유자를 확인만 하고 그대로 두었다 - 64화가 동시에
  렌더 중이었다)
- 렌더 횟수: **1회** (스틸 선점검에서 결함 1건을 렌더 전에 미리 잡아 수정했기 때문)
- 최종 mp4: 1385프레임 / 1080x1920 / 30fps (ffprobe 실측, 계산값과 프레임 수 정확히 일치)

## 4. 스틸 선점검에서 잡은 결함 (렌더 전에 발견·수정)

전체 렌더를 걸기 전에 `remotion still`로 구간별 시작·중간·끝 지점 총 17장을 뽑아 확인했다.

1. **`FingerprintSwirl` 최외곽 루프에 뾰족한 스파이크가 생김.** `variantSeed=3`(s7 최종
   클로즈업)에서 특정 (seed, loopIndex, pointIndex) 조합의 지터 값이 커서 최외곽 루프
   가장자리 한 지점이 뾰족하게 튀어나와 안테나처럼 보였다(`s7_mid.png`에서 발견). 지터
   진폭 계수를 `r * 0.2` -> `r * 0.13`으로 줄여 스파이크를 없애고, 재렌더 없이 스틸만
   다시 뽑아 s1/s6/s7에서 모두 매끈해진 것을 확인한 뒤 실제 렌더를 진행했다(원칙 5 "가벼운
   검증" - 컴포넌트 파일 1개, 숫자 상수 하나만 바꾼 예측 가능한 변경이라 전체 회귀 없이
   실제로 고친 프레임만 다시 확인).

## 5. 렌더 후에야 발견한 결함

**없음.** 최종 mp4에서 스틸 선점검 때(지터 수정 이후) 확인하지 못했던 새로운 결함은
발견되지 않았다. 장면 전환 경계(인트로/제목카드, s2->s3, s4->s5, s6->s7, s7->아웃트로)를
포함해 총 30개 프레임을 최종 mp4에서 직접 추출해 재확인했고, 캐릭터 잔상·라벨 겹침·자막
이탈이 관찰되지 않았다.

## 6. 검수 체크리스트 (관찰 기록)

- [x] **자막이 화면 밖으로 나가지 않는가** - f001~f030 전체(인트로부터 아웃트로까지 30개
  프레임) 확인. 가장 긴 문장(s5 "근데 이 과정에서 자궁 속 양수의 미세한 흐름이나...")도
  2줄로 자연스럽게 줄바꿈되어 좌우 여백 안에 들어옴.
- [x] **장면 전환 시 캐릭터 잔상** - s2->s3 경계(abs 328/330), s6->s7 경계(abs 1116/1118)
  에서 이전 장면 요소가 옅게 겹치는 크로스페이드(SceneSwitcher 기본 6프레임=0.2초) 외의
  잔상은 관찰되지 않음.
- [x] **등장 전 요소가 점처럼 남아 있지 않은가** - s1의 `FingerprintSwirl`은
  `drawProgress<=0.001`이면 아예 `null`을 반환하도록 컴포넌트 자체에 구현돼 있어(등장 전
  프레임에서) 점 잔상이 없음을 코드 레벨과 s1_start 스틸(프레임 123, 팝인 시작 직후) 양쪽
  으로 확인.
- [x] **라벨이 화면 밖에서 잘리지 않는가** - "표피"(s3, 블록 위 y=624)·"진피"(s3, 블록 아래
  y=1228)·"양수의 미세한 흐름"(s5, y=330)·"쌍둥이 지문 비교"(s6, y=252) 4개 라벨 전부
  화면 안, 블록·캐릭터와 겹치지 않음.
- [x] **요소끼리 겹치지 않는가** - s6에서 캐릭터(top=340)와 라벨(y=252), 라벨과 카드
  (y=1010) 사이 여백 확인. s1에서 손끝이 카드를 누르는 동안만 겹치고(의도된 연출), 손을
  뗀 뒤(s1_lift/s1_end)에는 카드와 분리됨.
- [x] **화면 아래쪽 여백이 과다하지 않은가** - s6은 캐릭터+제목+카드+캡션까지 화면 세로
  대부분을 채움. s3/s4/s5/s7은 다이어그램 중심에 caption이 하단에 위치해 하단 공백이 크지
  않음.
- [x] **음량이 충분한가** - `ffmpeg loudnorm` 실측: Input Integrated -13.6 LUFS, Input True
  Peak -1.9 dBTP(클리핑 없음). 효과음(ink_splat, `Episode.tsx`의 `volume={0.65}` 적용)
  피크 -6.5dB는 내레이션(각 구간 `volume={1.6}` 적용) 피크(-0.9~0.0dB) 대비 항상 낮아,
  두 소스를 각각 실제 volume 배율을 적용해 개별 측정한 결과 SFX가 내레이션을 넘지 않음을
  확인했다(내레이션과 SFX가 겹치는 구간이라 믹스된 최종 오디오 대신 소스 파일 개별 측정 방식
  사용 - 원칙 7).
- [x] **자막이 프로필 스타일(폰트·크기·위치)을 따르는가** - `Caption`/`CAPTION_STYLE`을
  그대로 사용, 로컬 오버라이드 없음.
- [x] **캐릭터 윤곽선이 배경과 명확히 구분되는가** - 전 장면 `PlainBg`(밝은 하늘색 계열,
  s5만 옅은 물빛 톤)라 어두운 배경 대비 이슈 없음.

## 7. 프로필(general) 추가 체크

- [x] **일상 체감 있는 궁금증인가** - "지문 도장 찍어보다 왜 사람마다 다른지 궁금해짐"은
  누구나 겪어본 상황(도장·지문인식)에서 출발해 순수 백과사전형 지식으로 흐르지 않음.
- [x] **어미가 친근한 대화체를 유지했는가** - "~거든요", "~줘요", "~인 거예요" 등 확인,
  유아어·학술 문어체 없음.
- [x] **전문용어(표피/진피)가 등장 자리에서 바로 풀렸는가** - "손끝 피부 겉면, 그러니까
  표피"로 즉시 풀이됨. 용어는 표피·진피 2개로 한 편당 상한(2개) 이내.
- [x] **자막 한 줄이 한국어 20자를 넘지 않는가** - `wrapCounts` 기본값(20자) 그대로 사용,
  개별 오버라이드 없음.
- [x] **60초 상한을 넘지 않는가** - 본편만 39.067초로 상한 이내.

## 8. 배포

기술적 검증(위 6·7절)을 통과해 즉시 배포했다.

- 배포 경로: `/home/lee/project/shorts/ko/[65화] 쌍둥이도 지문이 다른 이유.mp4`
- md5 대조: 렌더 산출물과 배포본 md5 일치 확인 완료
- 배포 후 `episodes/general-ep65-fingerprint/out/episode-ko.mp4` 삭제 완료(`out/frames-ko/`,
  `out/stills/`는 검수 근거로 보존)
