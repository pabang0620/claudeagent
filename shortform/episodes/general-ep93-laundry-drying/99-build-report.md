# 93화 "젖은 빨래가 마르는 이유" - 빌드 보고

## 산출물

- 배포 경로: `/home/lee/project/shorts/ko/[93화] 젖은 빨래가 마르는 이유.mp4`
- 실측 재생시간: 44.629초 (ffprobe, 1337프레임 @ 30fps)
- 배포 시 md5 대조: `out/episode-ko.mp4`와 `shorts/ko/...` 파일의 md5가 동일함을 확인(e083bb346c33ac7ee5d9cbc10011b92d)한 뒤 `out/`의 mp4를 삭제했다. `out/frames-ko/`, `out/stills/`는 검수 기록으로 남겨뒀다.
- 영어판은 요청받지 않아 만들지 않았다(원칙 6, 오케스트레이터 명시 지시).

## 자산 재사용/신규

### 재사용 (REGISTRY 대조 완료)
- `character/Actor`, `character/BustActor`, `character/poses`(idle, surprised, cheer 계열 blend)
- `backgrounds/PlainBg`
- `scenes/Caption`, `assets/anim`(clamp01, progress, sway)
- `props/ThemedIcon`(sun, wind 아이콘)
- `audio/intro_ding.mp3`, `audio/outro_ding.mp3`(공용 브랜드 SFX)
- `audio/wax_tap.mp3`(REGISTRY "작고 부드러운 재료를 다듬어 마무리하는 무성 동작 전반 재사용 가능" - 빨래집게로 옷을 거는 마무리 동작에 재사용, 신규 합성 없음)

### REGISTRY 사전 조회 결과
- 66화 `FogLayerDiagram`(공기 중 수증기가 식어 안개로 응결), 53화 `CondensationDroplets`(차가운 표면에 물방울로 맺힘) 확인 완료 - 둘 다 기체→액체(응결) 방향이라 이 화가 다루는 액체→기체(증발) 방향과 정반대 구조라 재사용 불가.
- `WetFabricLightDiagram`(옷이 젖으면 색이 진해지는 이유, 빛의 반사/흡수)도 확인했으나 다루는 물리가 전혀 달라 재사용 대상 아님.

### 신규 - `props/EvaporationDiagram.tsx` (1개)
- REGISTRY 등록 완료(파일 끝 "추록 2" 절에 병행 작업 안전 원칙에 따라 덧붙임 - 92화 작업과 동시 진행 중이라 기존 표는 건드리지 않음)
- `assets/props/index.ts`에 export 추가 완료(기존 줄 유지, 파일 끝에 추가)
- 독립 레이어 4종(escapeProgress/saturationProgress/windProgress/heatProgress) - 대본이 지정한 설계를 그대로 구현
- 물 분자는 잔류 2개 + 탈출 1개, 총 3개 큰 원형 글리프로만 표현(오케스트레이터 지시 - 점을 잔뜩 뿌리지 않음). heatProgress가 높아져도 개수는 늘리지 않고 탈출 분자의 주기·상승 높이만 키움
- 포화(haze 면)와 환기(왼쪽부터 쓸려나가는 폭)는 점이 아니라 면의 밀도·폭 변화로만 대비
- 부속 `SunRaysBadge`/`WindBadge`(ThemedIcon 감싼 절대좌표 배지, `<svg>` 바깥 형제로 배치 - SVG 중첩 위치 지정 무시 결함 예방)
- 빨랫줄+옷 실루엣(`Rope`/`ShirtSilhouette`)은 대본 자산 목록 메모대로 에피소드 로컬(`scenes.tsx`)에 두고 라이브러리로 승격하지 않았다(단순 도형, 참고 이미지 없음 - 원칙 0-1 벡터화 대상 아님)

## 언어별 실측 길이

한국어판만 제작(원칙 6). 구간별 실측 길이는 `02-script-final-ko.md` 참고.
- s2: 3.432s / s3: 6.672s / s4: 6.576s / s5: 6.840s / s6: 5.328s / s7: 4.536s
- 본편(s1 무성 2.5s 포함) 합계: 37.47초, 전체(Intro+TitleCard+본편+Outro): 44.567초(실측 mp4 44.629초)

## 검수 체크리스트 (관찰 기록)

- [x] **자막이 화면 밖으로 나가지 않는가**: f001~f018(1337프레임 중 대표 18프레임, 각 구간 시작/중간 및 전환 경계) 전체 확인. 모든 자막이 좌우 여백을 두고 화면 안에 들어옴. 가장 긴 문장("바람이 불면 그 꽉 찬 공기를 계속 새 공기로 바꿔줘서...")도 wrapCounts(20자) 기준으로 2줄 이내로 자연스럽게 줄바꿈됨.
- [x] **장면 전환 시 캐릭터 잔상**: s1→s2 경계(f005, 전역frame198)와 s2→s3 경계(f007, 전역frame319)에서 크로스페이드 중 이전 장면이 자연스럽게 겹쳐 보이는 것 외 잔상·겹침 아티팩트 없음.
- [x] **등장 전 요소가 점처럼 남아있지 않은가**: S1의 오른쪽 셔츠(팝인)와 S2~S6의 다이어그램 레이어는 전부 opacity 기반 등장(scale 0 방식 아님) - f004(s1 초반)에서 오른쪽 셔츠가 아직 옅게 나타나는 중이지만 점처럼 남는 아티팩트 없음.
- [x] **라벨이 화면 밖에서 잘리지 않는가**: SunRaysBadge(f014, DIAG_X+DIAG_WIDTH-90=940+size120=1060 < 화면폭 1080)와 WindBadge(f012, DIAG_X-30=20 > 0) 둘 다 화면 안에 들어옴을 좌표 계산 및 스틸로 확인.
- [x] **요소끼리 겹치지 않는가**: f018(Outro)에서 벨 아이콘·텍스트·캐릭터 겹침 없음. S3~S6 다이어그램에서 물 분자 3개(잔류2+탈출1)가 서로 겹치지 않는 고정 앵커 좌표 사용.
- [x] **화면 아래쪽 여백이 과다하지 않은가**: 스틸 선점검에서 3건 발견해 렌더 전 수정함(아래 "스틸 선점검에서 잡은 결함" 참고). 수정 후 f004~f018 재확인 - S1은 ground를 1250→1400으로 낮추고 캐릭터를 1080으로 키워 화면 하단 73%까지 채움, S7은 rope를 640→760으로 낮추고 옷 scale을 1.3→1.6으로 키워 캡션까지의 간격을 좁힘, S3~S6 다이어그램은 폭을 820→980으로 키워 세로 여백을 줄임.
- [x] **음량이 충분한가**: `ffmpeg -af loudnorm=print_format=summary` 측정 - Input Integrated -13.5 LUFS, Input True Peak -2.4 dBTP. 클리핑 없이 충분한 레벨.
- [x] **자막이 프로필의 스타일(폰트·크기·위치)을 따르는가**: `assets/theme.ts`의 `CAPTION_STYLE`/`FS.caption`을 그대로 사용(하드코딩 없음). f006/f008 등에서 흰 글자+검은 외곽선, 화면 하단에서 위로 약 23% 지점 위치 확인.
- [x] (한국어판만 제작이라 언어별 대조 항목 해당 없음)
- [x] **캐릭터 윤곽선이 배경과 명확히 구분되는가**: 전 구간 밝은 배경(C.sky/C.room/C.paper)만 사용해 어두운 배경(C.night 계열) 장면이 없음 - 해당 문제 발생 여지 없음.

### 프로필(general) 추가 체크
- [x] 소재가 "겪어봤지만 검색까진 안 해본 사소한 궁금증"인가 - "빨래가 마르는 이유"는 매일 겪는 일상 현상이라 부합.
- [x] 어미가 친근한 대화체("~거든요", "~것 같아요")를 유지 - 대본 s3~s7 전부 "~거든요"/"~요" 어미 확인.
- [x] 전문용어 즉시 풀이 - "증발"이라는 단어 자체를 쓰지 않고 "물 분자가 공기 중으로 날아간다"는 쉬운 말로 전 구간 서술됨(용어 노출 없음).
- [x] 자막 한 줄 20자(한국어) 이내 - f006/f008/f010/f012/f014/f016에서 전부 준수 확인.
- [x] 60초 상한 - 44.63초로 상한 내.

## 오디오 SFX 객관적 검증

- **효과음 배치 프레임**: `S1_HANG_SFX_AT_FRAME=20`(s1 로컬) → 전역 프레임143 → 예상 시각 4.767s. 최종 mp4에서 s1 구간(내레이션 없는 무음 구간)의 RMS 엔벨로프를 0.1초 단위로 측정한 결과 4.8s 지점에서만 뚜렷한 피크(진폭 9767, 그 앞뒤 구간은 전부 0)가 관찰됨 - 예상 시각과 약 33ms 오차로 일치.
- **효과음 vs 내레이션 피크 비교**: `Episode.tsx`가 실제로 곱하는 volume 값을 적용해 각 소스를 개별 측정(내레이션과 겹치는 구간이 없어 최종 믹스에서도 직접 확인 가능했지만, 원칙에 따라 소스 파일 기준으로도 별도 측정) - `wax_tap.mp3`(volume=0.8) max_volume -7.5dB vs `ko_s2.mp3`(volume=1.6, 내레이션 대표) max_volume -0.2dB. 효과음이 내레이션보다 7.3dB 낮음 - 원칙 7 충족.

## 스틸 선점검에서 잡은 결함 (렌더 전 발견·수정, 원칙 5)

1. **S1(빨랫줄+캐릭터) 하단 여백 과다**: 처음 size=900/ground=1250(기본값)으로 스틸(f150/f198)을 뽑아보니 콘텐츠가 화면 위쪽 2/3에만 몰리고 하단 1/3이 비어 보였다. 무성 장면이라 캡션이 없어 SAFE_BOTTOM 경계(1400)까지 안전하게 채울 수 있음을 확인하고 ground=1400/size=1080으로 조정. 재스틸(f150_v2/f198_v2)로 floor 경계가 실제로 y≈1400으로 이동했음을 픽셀 단위(python PIL)로 확인.
2. **S7(빨래 다림) 상하 여백 과다**: rope_y=640/scale=1.3으로 뽑은 스틸(f1200)에서 옷이 화면 중앙에 작게 몰리고 캡션까지 빈 공간이 컸다. rope_y=760/scale=1.6으로 키워 재스틸(f1200_v2) 확인 - 캡션까지의 간격이 좁혀짐.
3. **S3~S6(EvaporationDiagram) 다이어그램 폭 협소**: width=820으로 뽑은 스틸(f525/f650 등)에서 다이어그램 아래 캡션까지 여백이 컸다. width=980으로 키운 뒤 재스틸(f650_v2)로 화면 폭에 맞게 채워짐을 확인, 좌우 여백도 남아 화면 밖으로 나가지 않음을 확인.

## 렌더 후에야 발견한 것 (진단용, 결함 아님으로 판정)

- 렌더 전 스틸 선점검 중 f319(예상: s3 시작, escapeProgress=0)에서 예상과 달리 전신 캐릭터(cheer 포즈)가 나타나 한때 "SceneSwitcher 타이밍 버그"로 의심했다. `BustActor`/`S1HangProud`에 임시 디버그 라벨(`S1`/`S2` + 로컬 프레임 텍스트)을 삽입해 재검증한 결과, 실제로는 **S2(BustActor) 자체가 렌더링한 화면**이었다 - `BUST_VIEWBOX`가 크롭하는 영역이 실제로는 전신이 거의 다 보일 만큼 넓어(`'236 132 780 780'`, hip까지 포함), "바스트샷"이라는 이름과 달리 원래부터 전신에 가깝게 렌더링되는 것이 이 컴포넌트의 기존(다른 화들과 동일한) 동작임을 기존 배포작(91화)에서도 동일하게 재현되는 것으로 교차 확인했다. **이 화 고유의 결함이 아니라 공용 `BustActor` 컴포넌트의 기존·전역적 특성**이라 이 화에서 별도로 수정하지 않았다(공용 컴포넌트 수정은 이 작업 범위 밖 - 92화와 병행 작업 중이라 공용 컴포넌트를 손대면 영향 범위가 이 화를 벗어난다). 디버그 라벨은 확인 직후 원복해 최종 렌더에는 포함되지 않았다.
- 그 외 렌더 후 프레임 검수(f001~f018)에서 새로 발견된 결함은 없었다 - 위 3건 전부 스틸 선점검 단계에서 잡혀 렌더 전에 수정됐다.

## 렌더 횟수

- `remotion still` 선점검: 다수(구간별 시작/중간 프레임 + 결함 수정 후 재검증 + 디버그 진단용)
- 최종 전체 렌더: **1회** (ko, 1337프레임)
