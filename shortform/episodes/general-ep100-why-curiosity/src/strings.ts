/** 이 화(general-ep100, "사람이 자꾸 궁금해지는 이유", 21~100화 시리즈 마지막 화) 전용
 *  화면 문구. 씬 컴포넌트 안에 문자열을 박지 않고 이 언어별 테이블에서만 읽는다.
 *
 *  영어 채널(Whymo) 운영 중단(2026-09-02, 오케스트레이터 명시 지시)으로 한국어만
 *  만든다. STRINGS 타입은 다른 화와 같은 Locale 패턴을 유지한다.
 *
 *  100화이지만 자축·감사 인사·구독 유도 문구는 대본에 없어(오케스트레이터 지시)
 *  인트로·아웃트로 문구도 다른 화와 동일한 표준 문구를 그대로 쓴다 - 특별 멘트를
 *  새로 추가하지 않는다.
 */
export const STRINGS = {
  ko: {
    title: '사람이 자꾸 궁금해지는 이유',
    outroNextTitle: '다음 편',
    outroNextHint: '다음 편에서 또 다른 궁금증이 풀려요!',
  },
} as const;

export type Locale = keyof typeof STRINGS;
