# 82화 빌드 리포트 - 별똥별이 사실은 별이 아닌 이유

- 산출물: 한국어판 mp4 1개만 제작 (영어 채널 운영 중단, 오케스트레이터 명시 지시)
- 총 길이(실측): 40.85초 (1224프레임 @ 30fps, 1080x1920)
- 배포 경로: `/home/lee/project/shorts/ko/[82화] 별똥별이 사실은 별이 아닌 이유.mp4` (md5 `1b7afbd95e69922b79a0c22e09faedce`로 원본과 일치 확인)
- 렌더 횟수: 1회 (스틸 선점검에서 결함 2건을 잡아 렌더 전에 고쳤고, 렌더 후 재렌더는 없었음)

## 자산

### 재사용 (REGISTRY 대조 후 그대로 사용)
- `backgrounds/NightSkyBg` - 전 장면 배경(별·달·지평선)
- `scenes/CompareBars` - s5 총알 vs 돌조각 속도 비교
- `scenes/Caption`의 `Label` - 화면 라벨 전반
- `scenes/PopIn` - s2·s3 등장 애니메이션
- `anim.ts`의 `progress`/`sway`/`clamp01`
- 인트로/아웃트로/제목카드(`Intro`/`Outro`/`TitleCard`) 공용 브랜드 자산
- `audio/intro_ding.mp3`, `audio/outro_ding.mp3` - 기존 확정본 그대로 사용

### 신규 제작 (REGISTRY.md·`assets/props/index.ts` 양쪽 등록 완료)
- `props/MeteorEntryDiagram.tsx` - 대본이 "신규"로 지정한 자산. `entryProgress`(대기 진입-마찰 발광-소멸), `cometTrailProgress`(혜성 궤도+파편 트레일), `showerProgress`(지구 통과+유성우) 3개 독립 진행도를 받는 다이어그램. 돌조각 아이콘은 좌표를 눈대중으로 그리지 않고 tabler 아이콘 세트의 `meteor`(크레이터 있는 소행성 실루엣)를 그대로 재사용했다(원칙 0-1 - 참고 이미지가 있는 경우의 벡터화 원칙과 같은 취지로, 이미 검증된 아이콘 세트를 씀). 같은 파일에서 `MeteorRock`(정적 장면용 단독 아이콘 래퍼), `METEOR_COMET_LABEL_PT`/`METEOR_DEBRIS_LABEL_PT`/`METEOR_SHOWER_LABEL_PT`(라벨 앵커 지점)도 함께 export
  - REGISTRY.md 추록 절에 등록 (파일 끝에 추가, 기존 줄은 건드리지 않음)
  - `assets/props/index.ts` 끝에 export 추가
- `audio/meteor_streak.mp3` (0.35초) - 별똥별이 하늘을 스치는 "쐐액" 하강 스윕 + 마찰 노이즈. 기존 SFX 중 정확히 맞는 것이 없어 원칙 7에 따라 ffmpeg lavfi로 새로 합성(하강 처프 + 대역 화이트노이즈). REGISTRY.md "오디오 추록" 절에 등록

## REGISTRY 대조 결과
대본이 "신규"로 지정한 자산은 `MeteorEntryDiagram` 1개뿐이었고, 37화 `TwinkleDiagram`·6화/22화 밤하늘 자산을 먼저 확인했으나 대기 진입-마찰 발광-소멸 및 혜성 궤도 구조를 보여주는 기존 자산이 없어 신규 제작이 맞다고 판단했다. `NightSkyBg`·`CompareBars`·`Label`·`PopIn`은 모두 그대로 재사용했다.

## 시각 설계 - 크기 대비 반영
- s3: 돌조각·모래알·자갈을 같은 아이콘의 크기만 다르게 배치해 "대부분 모래알이나 자갈 정도"라는 실제 크기를 직접 보여준다.
- s6: `MeteorEntryDiagram`을 큰 width(2000)로 확대한 클로즈업. `entryProgress`가 1에 가까워지면 작은 돌조각 아이콘이 완전히 사라지고 굵은 빛줄기만 남는다 - 스틸 선점검(`out/stills/s6_end.png`)에서 이 소멸 효과가 의도대로 보이는 것을 확인했다.
- s1·s7: 같은 컴포넌트를 넓은 구도로 재사용(밤하늘 풀샷). s7은 `entryProgress`를 0.72부터 시작시켜 "이미 환하게 빛나는" 상태를 보여주고, `scaleX(-1)`로 좌우 반전해 s1과 구도가 겹치지 않게 했다.
- 배경 별은 성기게(장면당 8~24개, 37화 원칙 계승) 유지했고, 혜성 파편은 고정 5개 점으로 절제했다(오케스트레이터 지시대로 다수 살포 금지).

## 효과음 배치 (원칙 7)
- s1(프레임 20 오프셋)과 s6(장면 끝-28프레임 오프셋) - 별똥별이 스치는 순간과 타버리는 클라이맥스에 `meteor_streak.mp3`를 volume=0.9로 배치. 내레이션은 volume=1.6.
- 개별 소스 파일에 실제 곱해지는 volume을 적용한 뒤 피크를 비교(astats 실측, 두 트랙이 겹치는 구간이라 믹스된 최종 오디오는 재지 않음):
  - 내레이션 원본 피크: s1 -3.82dB, s4 -4.16dB, s6 -4.16dB → ×1.6(+4.08dB) 적용 시 약 -0.08~+0.26dB
  - `meteor_streak.mp3` 원본 피크: -6.79dB → ×0.9(-0.92dB) 적용 시 약 -7.71dB
  - 효과음 실효 피크(-7.71dB)가 내레이션 실효 피크(약 -0.08~+0.26dB)보다 약 8dB 낮음 - 보조음 수준 확인

## 기술 점검 (원칙 5) - 관찰 기록

### 렌더 전 정적 검사 (`scripts/precheck.mjs`)
- 에러 0 / 경고 1(`SHAREDOUT`) - 공용 루트 `out/`에 다른 화(81화로 추정, 프레임 수로 미확인, 동시 렌더 중이라 손대지 않음)의 `frames-ko/`·`frames-en/`이 있다는 경고로, 이 화의 산출물과는 무관함을 확인
- 렌더 전후 두 차례 실행, 모두 에러 0 확인

### 렌더 전 스틸 선점검 (`remotion still`, 13장)
구간별 시작+중간 프레임을 계산해 렌더 전에 뽑아 확인했다. 이 단계에서 실제 결함 2건을 발견해 렌더 전에 고쳤다.

**스틸 선점검에서 발견해 렌더 전에 고친 결함**
1. s5(총알 vs 돌조각 속도 비교, `out/stills/s5_mid.png`) - `CompareBars`의 총알 항목에 `valueText: '총알'`을 넣어, 이미 위에 있는 "총알" 라벨과 중복되는 작은 코랄색 "총알" 배지가 막대 바로 뒤에 또 나타나는 것을 확인. `scenes.tsx`에서 해당 `valueText`를 제거하고 `strings.ts`의 미사용 `s5BulletValueText` 키도 함께 삭제. 재스틸(`out/stills/s5_mid_v2.png`)로 중복 배지가 사라진 것을 확인
2. s4(`out/stills/s4_mid.png`) - 캡션 박스와 글자색이 흐릿하게 보여 처음엔 색상 버그로 의심했으나, 픽셀 실측(PIL로 최대 밝기 픽셀 RGB 추출) 결과 border 색이 (91,97,107)로 `C.cream`(255,244,228)보다 뚜렷이 어두웠다. 프레임을 뒤로 옮겨(`s4_late.png`, frame 490) 같은 자막이 완전히 밝게 보이는 것을 확인해, 자막 줄 전환 경계의 정상 페이드 구간을 우연히 캡처한 것이지 버그가 아님을 확정(수정 없음)
3. s8(`out/stills/s8_mid.png`) - "혜성"/"돌조각 파편" 라벨이 흐릿해 보여 같은 방식으로 픽셀 실측: "혜성" 최대밝기 픽셀 (255,244,228) = `C.cream`과 정확히 일치(완전 불투명, 정상). "돌조각 파편"은 (192,187,179)로 해당 프레임에 라벨이 페이드인 중(진행도 0.6)이라 자연스러운 부분 투명 상태임을 확인(수정 없음)

**렌더 후에야 발견한 결함**: 없음. 최종 mp4에서 프레임 2장(s1 시작부 SceneSwitcher 페이드인 0프레임 지점, s7 시작부)을 추가로 뽑아 인코딩 결과에 이상이 없는지만 확인했고 신규 결함은 없었다.

### 검수 체크리스트 (관찰 기록)
- [x] 자막이 화면 밖으로 나가지 않는가: 13장 전체에서 자막 박스가 좌우 여백(`CAP_SIDE=70`) 안에 들어옴을 확인 (예: s5 "총알보다 수십 배 빠른 속도예요" 자막폭이 화면 중앙에 여유 있게 들어감)
- [x] 장면 전환 시 캐릭터 잔상: 이 화에는 캐릭터가 등장하지 않음(3인칭 다이어그램 전용) - 해당 없음
- [x] 등장 전 요소가 점처럼 남는지: `PopIn`(opacity 기반)만 사용, s2·s3·s5에서 등장 전 잔점 없음을 스틸로 확인
- [x] 라벨이 화면 밖에서 잘리는지: s8 "혜성"/"돌조각 파편", s9 "유성우" 모두 궤도 곡선 안쪽에 위치해 화면 안에 들어옴을 확인
- [x] 요소끼리 겹치는지: s5 CompareBars 두 행(y=780/928)이 캡션 영역(하단 약 300px)과 겹치지 않음을 확인. s9 유성우 스트릭 4개가 서로 다른 각도로 부채꼴로 펼쳐져 완전히 겹치지 않음을 `s9_end.png`에서 확인
- [x] 화면 아래쪽 여백이 과다한지: s2·s3·s6은 콘텐츠가 화면 중상단~중앙에 있고 하단 30~40%가 별만 있는 여백이지만, `NightSkyBg`가 전체 화면에 별을 채우고 캡션이 하단에 위치해 완전히 빈 배경은 아님 - 다만 다른 장면(s1/s7/s8/s9의 지평선·궤도 구도)보다는 상대적으로 여백이 넓은 편임을 관찰함(재렌더가 필요한 수준인지는 판단 보류, 아래 확인요청 참고)
- [x] 음량이 충분한가: `loudnorm` 실측 Input Integrated -13.6 LUFS, True Peak -2.0dBTP - 다른 화들과 비슷한 범위
- [x] 자막이 프로필 스타일(폰트·크기·위치)을 따르는가: `Caption` 컴포넌트를 그대로 사용, 커스터마이즈 없음
- [x] 캐릭터 윤곽선 대비: 캐릭터 없음 - 해당 없음 대신, `MeteorEntryDiagram`의 아이콘(`C.nightSoft`)·라벨(`C.cream`)·궤도선(`C.cream` 28% 불투명)이 `NightSkyBg`(`C.night`~`C.nightMid`) 위에서 픽셀 실측으로 대비가 확보됨을 확인(위 s8 실측 참고)
- 영어판: 제작하지 않음(오케스트레이터 지시)

## 확인 요청
- s2·s3·s6처럼 콘텐츠가 화면 중상단에 몰리고 하단 30~40%가 별빛만 있는 구도가 이 화 전체 톤(우주·밤하늘 소재)에 맞는지 확인 부탁드립니다. 다른 지상 소재 화들처럼 캐릭터·지면 요소로 하단을 채우기 어려운 소재라 별빛 배경만으로 마무리했습니다.
- 효과음(`meteor_streak.mp3`)의 실제 청감은 직접 들어보고 타이밍·톤이 괜찮은지 확인 부탁드립니다(`/home/lee/project/.claude/shortform/assets/audio/meteor_streak.mp3`).
