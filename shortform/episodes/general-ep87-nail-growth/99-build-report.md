# 빌드 보고 - general-ep87-nail-growth (손톱이 발톱보다 빨리 자라는 이유)

**한국어판 1개만 제작했다** (오케스트레이터 명시 지시 - 영어 채널 Whymo 운영 중단, 2026-09-02).

## 자산

### 재사용 (신규 제작 없음)
- 캐릭터: `Actor`, `BustActor`, `POSES.idle` / `POSES.surprised` / `POSES.crouch`(전신 히어로샷 관례는 general-ep33과 동일하게 `size=1400` + 기본 `GROUND` 재사용)
- 배경: `PlainBg`
- 씬 컴포넌트: `Caption`, `Label`, `CompareBars`, `PulseRing`(Effects.tsx), `MotionSwoosh`, `SceneSwitcher`, `TitleCard`, `FontLoader`
- 소품: `Finger`(props/Hand.tsx), `ThemedIcon`(`arrow-up`)
- 브랜드: `Intro`, `Outro`
- 타임라인/애니메이션 유틸: `sceneFrames`/`sceneStarts`/`buildCaptions`/`wrapCounts`/`mouthAt`/`mouthProp`(timeline.ts), `progress`/`blendPose`/`clamp01`(anim.ts), `handPos`/`RIG`/`FEET_VB`(캐릭터 리그 - 손톱깎이 소품을 손끝에 정확히 붙이는 데 재사용)
- 오디오: `intro_ding.mp3`, `outro_ding.mp3`(Intro/Outro 내장), `ui_tap.mp3`(손톱을 깎는 순간의 "톡" 클릭음으로 재사용 - REGISTRY 7절에 "화면 UI 탭·버튼 누름 동작 전반 재사용 가능"으로 이미 등록되어 있어 새로 합성하지 않았다)

### 신규 제작 (REGISTRY 등록 완료)
- `assets/props/NailRootDiagram.tsx` - 손끝 단면(손톱판 + 손톱이 자라는 뿌리) 클로즈업 소품. `bloodFlowProgress`/`stimulateProgress` 독립 진행도, `f`로 맥동. `assets/REGISTRY.md`(추록 절, BubbleFilmDiagram 다음 줄에 추가)와 `assets/props/index.ts`(파일 끝에 추가) 양쪽 다 등록 완료 - 기존 줄은 건드리지 않고 끝에만 덧붙였다.
- `NailClipper`(에피소드 로컬, `scenes.tsx` 안에 정의) - s1에서 쓰는 작은 손톱깎이 장식 아이콘. 참고 이미지 없이 도형 3개(집게+지렛대+힌지)로 단순화한 일회성 장식이라 레지스트리에 등록하지 않았다(재사용 가치가 낮다고 판단, 코드 주석에 사유 기재).

## 언어별 실측 길이

한국어판만 제작. 구간별 실측(edge-tts WordBoundary, `ko_words.json`):

| 구간 | 실측 발화 길이 | 여백 | 최종 프레임 |
|---|---|---|---|
| s1(무성) | - (대본 지정 2.6초) | 0 | 78f |
| s2 | 5.016s | +0.6s(리액션→설명 전환) | 168f |
| s3 | 3.168s | +0.2s | 101f |
| s4 | 6.120s | +0.2s | 190f |
| s5 | 6.936s | +0.2s | 214f |
| s6 | 5.472s | +0.2s | 170f |
| s7 | 5.184s | +0.2s | 162f |

본편(s1~s7) 합계 1083프레임(36.1초) + 인트로 69f + 타이틀카드 54f + 아웃트로 90f = **총 1296프레임(43.2초)**, 60초 상한 이내. mp4 실측 재생시간은 43.264초(컨테이너 인코딩 반올림 차이, 프레임 수 기준과 거의 일치).

## 렌더 횟수

- `precheck.mjs`: 2회 실행, 둘 다 에러 0 / 경고 1(`SHAREDOUT` - 공용 루트 `out/`에 있던 다른 화의 옛 산출물, 이 화가 만든 것이 아님을 타임스탬프로 확인 후 그대로 둠)
- `remotion still` 스틸 선점검: 8개 대표 프레임 1차 + 결함 수정 후 5개 재확인(v2) = 총 13회 still 호출. **결함 3건을 이 단계에서 모두 잡았다**(아래 참고). 전체 mp4 렌더 없이 코드만 수정.
- 전체 mp4 렌더(`render.mjs ... ko`): **1회**. 스틸 선점검에서 결함을 먼저 잡았기 때문에 전체 렌더는 한 번으로 끝났다.

## 검수 결과 (관찰 기록, 원칙 5 - "통과" 같은 자체 최종판정 표현 없이 사실만 기록)

### 스틸 선점검에서 잡은 결함 (렌더 전에 발견, 전체 재렌더 없이 코드만 수정)

1. **자막/라벨이 화면 밖으로 잘림 (S3)**: `s3_bars.png`(pxPerUnit=210, x=210)에서 손톱 막대의 valueText "약 3mm/월"이 화면 오른쪽 바깥으로 잘려 마지막 글자가 안 보였다. `pxPerUnit`을 140으로, `x`를 170으로 줄여 재확인(`s3_bars_v2.png`) - "약 3mm/월" 전체가 화면 안에 들어옴을 확인.
2. **요소끼리 겹침 (S4)**: `s4_root.png`에서 Label "손톱이 자라는 뿌리"가 NailRootDiagram의 손톱/뿌리 도형과 정확히 겹쳐 텍스트와 도형 윤곽선이 서로 가려 읽기 어려웠다. Label을 다이어그램 위쪽 안전영역(y=300)으로 옮겨 재확인(`s4_root_v2.png`) - 텍스트와 도형이 겹치지 않고 분리됨을 확인.
3. **화살표 형태가 X(속설 표시 기호)처럼 보일 위험 (S5)**: `s5_blood.png`에서 NailRootDiagram의 혈류 화살촉을 열린 쉐브론(`<`/`>` 꺾은선) 2개로 그렸더니 두 화살촉이 뿌리 지점 근처에서 서로 마주보며 이 채널이 "틀림"을 뜻하는 데 쓰는 X 표시처럼 보일 수 있는 모양이 됐다. `NailRootDiagram.tsx`에서 화살촉을 채워진 삼각형(polygon) 1개씩으로 바꿔 재확인(`s5_blood_v2.png`, `s5_blood_late_v2.png`) - 두 화살이 뿌리 쪽으로 수렴하는 형태로만 보이고 X로 오인될 소지가 사라짐을 확인.

### 렌더 후에야 확인한 항목 (스틸에서는 안 보이거나 스틸 대상이 아니었던 것)

- **장면 전환 크로스페이드 경계 동작**: 최종 mp4에서 각 씬 시작 프레임(예: s3 시작 369프레임)을 그대로 추출해보니 직전 씬(s2 리액션 포즈)이 여전히 완전 불투명으로 보였다. `SceneSwitcher.tsx` 코드를 확인한 결과 xfade(6프레임)·fadeIn(8프레임) 크로스페이드 설계상 정상 동작(나가는 씬이 outA=1로 시작해 서서히 사라지고, 들어오는 씬이 inA=0으로 시작해 서서히 나타남)임을 확인 - 결함이 아니다. 각 씬 시작+10프레임(g001~g005)으로 다시 추출해 실제 그래픽이 정상 노출됨을 확인.
- **음량**: `ffmpeg loudnorm=print_format=summary` 측정 - Input Integrated -13.9 LUFS, Input True Peak -2.0 dBTP(클리핑 없음).
- **효과음 vs 내레이션 상대 음량**: `ui_tap.mp3`(volume=0.8 적용) 피크 -5.1dB, 내레이션 `ko_s2.mp3`(volume=1.6 적용) 피크 -0.5dB로 SFX가 내레이션보다 4.6dB 낮음을 확인(원칙 7 요구사항 충족). s1은 무성 구간이라 실제 재생 시 두 트랙이 겹치지 않지만, 동일한 개별 측정 방법을 적용했다.
- **화면 아래쪽 여백**: s1(전신 CROUCH 포즈, size=1400/기본 GROUND) 프레임에서 상하 여백이 다소 커 보였으나, 이미 배포된 general-ep33의 CROUCH 전신 히어로샷 프레임(`general-ep33-salty-sea/out/frames-ko/f001.png`)과 대조한 결과 이 채널에서 이미 쓰이고 있는 표준 프레이밍과 동일한 수준임을 확인 - 추가 수정 없이 그대로 뒀다.
- **캐릭터 윤곽선-배경 대비**: 전 장면이 밝은 톤 배경(`C.sky`/`C.room`/`C.paper`)만 사용해 기본 `C.ink` 스트로크로 충분히 구분됨을 f001~f021, g001~g005 전체 프레임에서 확인. 어두운 배경(`C.night` 계열)은 이 화에서 쓰지 않았다.
- **자막 화면이탈 전수 확인**: f001~f021, g001~g005 총 26개 프레임에서 자막 박스 텍스트가 좌우 안전영역(`CAP_SIDE`) 안에 들어오는 것을 확인.
- **자막 스타일**: 공용 `CAPTION_STYLE`(general 프로필 기본값 - 흰 글자+검은 외곽선, 화면 하단 23% 지점, 한국어 한 줄 20자 상한) 그대로 사용, 별도 오버라이드 없음.
- **영어판 문자열 누출**: 해당 없음(영어판 미제작, `precheck.mjs`의 `KO-STR` 검사도 `strings.ts` 외 한국어 리터럴 없음을 확인 - 에러 0).

### 프로필 추가 체크 (general.md 8절)

- 소재: "손톱은 자주 깎는데 발톱은 왜 안 자라지"는 일상에서 겪어봤지만 검색까진 안 해본 사소한 궁금증에 해당.
- 어미: "~거든요", "~있어요", "~봐요", "~셈이에요" 등 친근한 대화체 유지 확인(script-ko.json 원문 그대로 사용, 문장 수정 없음).
- 전문용어: 대본·화면 모두 "조모"(전문용어)를 그대로 노출하지 않고 "손톱이 자라는 뿌리"로 순화해 표현 - 풀이가 필요한 전문용어 자체가 등장하지 않음.
- 자막 한 줄 글자수: `wrapCounts(words, 'ko')` 기본값(20자) 그대로 사용.
- 60초 상한: 43.2초로 상한 이내.

## 배포

기술 점검(위 항목)을 마치고 즉시 `shorts/ko/`에 복사했다(md5 대조 완료, 원칙 5 - 승인 질문 없이 진행).

- 배포 경로: `/home/lee/project/shorts/ko/[87화] 손톱이 발톱보다 빨리 자라는 이유.mp4`
- 실측 재생시간: 43.264초
- 배포 후 `episodes/general-ep87-nail-growth/out/episode-ko.mp4`는 삭제했다(`out/frames-ko/`, `out/stills/`는 검수 기록으로 보존).

이 보고는 "이렇게 나왔습니다, 확인해주세요" 수준의 기술적 관찰 기록이다. "검수 통과"·"합격" 같은 최종 판정은 담지 않았고, 최종 판단은 사용자 몫이다.
