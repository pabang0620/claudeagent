# 대본 비평: general-ep01-untitled (브레인프리즈)

## 사전 확인
- 대본 파일(`02-script-v1.md`), `01-research.md`, 프로필(`general.md`) 모두 확보됨. 누락된 파일 없어 전체 검사(근거 대조 + TTS rate 반영 길이 재계산) 정상 수행.
- 대본 파일 구조상 "표"가 두 개(`장면` 표 + `내레이션 대조표`)로 나뉘어 있어, 이 둘을 s1~s5 기준으로 행 단위 병합해 판정함. `내레이션 대조표`의 행 수(5행, s1~s5)를 원본 행 수로 삼음.

---

## 문장 단위 판정

| # | 구간 | 원문(ko) | 판정 | 이유 | 압축안(참고) |
|---|---|---|---|---|---|
| s1 | 0:00-0:02.5 | 아이스크림을 급하게 먹다 보면 | 삭제 | 화면 지시가 "캐릭터가 아이스크림콘을 들고 크게 한입 베어 무는 동작"으로 정확히 같은 행위를 이미 보여줌. 화면 대조 검사 기준("장면 지시가 이미 보여주는 동작을 내레이션이 서술하면 삭제") 그대로 해당. s1을 빼도 s2의 화면 텍스트 "이마가 찌릿"만으로 "아이스크림 먹다가 이마가 아프다"는 상황이 전달돼 이해가 끊기지 않음 | - |
| s2 | 0:02.5-0:05 | (내레이션 없음) | 해당없음 | 원래 무성 구간. 화면 텍스트("이마가 찌릿")와 통증 아이콘이 정보를 단독으로 전달하므로 화면·목소리 둘 다 비어있는 구간이 아님. 문제 없음 | - |
| s3 | 0:05-0:09 | 근데 진짜 차가워진 건 입 안이거든요. | 유지 | 화면(s3)은 다이어그램에 파란 냉기 표시만 있고 텍스트 라벨이 없어, "차가운 곳은 입 안"이라는 의미 자체는 목소리가 아니면 전달되지 않음(단순 색 표시만으로는 "이게 왜 중요한지" 불명확). s2의 이마 통증 기대와 대비를 잇는 "근데"는 뻔한 연결어 목록(그런데 말이죠·자 그럼·여기서 잠깐·과연)에 해당하지 않는 최소 대비 표지. 현재 문장이 이미 최단 표현 | - |
| s4 | 0:09-0:13.5 | 입천장이랑 이마 감각을 같은 신경 한 가닥이 같이 맡고 있어서 | 압축 | 화면은 신경선이 그려지는 애니메이션만 보여주고 "왜 이게 의미 있는지"(같은 신경이라 서로 신호가 섞인다는 개념)는 목소리 없이 전달 안 됨 → 유지 필요. 다만 "같은"과 "같이"가 한 문장에 겹쳐 의미 중복("같은 신경"+"같이 맡고") | 입천장이랑 이마는 신경 하나가 같이 맡고 있어서 |
| s5 | 0:13.5-0:18.5 | 뇌가 입천장 신호를 이마 통증으로 착각하는 거예요. | 유지 | 화면은 신호가 잘못된 방향으로 튀는 화살표 애니메이션만 제공 - "뇌가 착각한다"는 결론(연구자료 #2, A등급)은 목소리로만 전달됨. 마지막 문장이라 핵심 사실 1회 전달 원칙에도 부합. 이미 군더더기 없는 최단 표현(불필요 수식어·반복 없음) | - |

근거 대조 결과: s3(연구자료 #1, A등급), s4·s5(연구자료 #2, A등급)와 정확히 대응. `01-research.md`에 없는 신규 수치·주장 없음. 등급 B(전대뇌동맥 부가 가설)는 대본에 아예 등장하지 않아 단정적 표현 문제도 없음.

---

## 화면 대조 결과

- **s1**: 화면(아이스크림을 크게 베어 무는 동작) ↔ 내레이션("아이스크림을 급하게 먹다 보면") 동작 중복 → 내레이션 삭제 권고 (위 표 참고)
- s2~s5: 화면(다이어그램·신경선·화살표 애니메이션)과 내레이션(개념 설명)이 서로 다른 층위의 정보를 전달하고 있어 중복 없음. 화면은 "어디서 무슨 일이 일어나는지"를 보여주고, 목소리는 "그게 왜 그런지"를 설명하는 역할 분리가 유지됨
- 화면·목소리 둘 다 정보가 없는 구간: 없음. s1 내레이션을 삭제해도 s1 구간 자체는 시각 정보(베어 무는 동작)가 남아있어 "빈 구간"이 되지는 않음

---

## 길이 재계산

- 기준: 한국어 초당 5.5음절 × TTS rate +20% = 초당 약 6.6음절 (어림값)
- v1 원본 내레이션 음절수: s1 13 + s3 15 + s4 25 + s5 21 = **74음절** → 발화시간만 약 11.2초. 여기에 마침표 무음(s3·s5 말미 각 0.7초=1.4초) + 구간 간 여백(0.2초×4구간=0.8초)을 더하면 약 **13.4초**. (참고: 대본 파일 자체 표기는 "약 19초"로 다소 차이 있음 - 화면 전환 여유 등 다른 가정이 포함됐을 수 있는 어림값 차이로 판단, 정밀 일치는 TTS 실측에서 확인 필요)
- 삭제(s1, -13음절) + 압축(s4, 25→20음절, -5음절) 반영 시: s3 15 + s4 20 + s5 21 = **56음절** → 발화시간 약 8.5초 + 무음(1.4초+0.8초) ≈ **10.7초 추정** (동일 방법론으로 재계산, 어림값)
- 짧아진 것 자체는 문제가 아님. 프로필 상한 60초 대비 여유가 큰 폭으로 늘었을 뿐, 되살릴 필요 없음
- 상한(60초) 초과 없음 - "추가로 더 쳐낼 후보" 제시 불필요

---

## 총평

- 삭제 1건(s1) / 압축 1건(s4) / 유지 2건(s3, s5) / 해당없음 1건(s2, 원래 무성 구간)
- 가장 큰 문제 유형: 화면-내레이션 동작 중복(s1) 1건. 나머지는 대체로 정보 층위가 분리되어 있어 중복이 적은 편
- 근거 미확인 주장: 없음 (전 문장이 `01-research.md` A등급 항목과 정합)
- 숫자 처리 원칙(내레이션이 숫자를 읽지 않는다) 위반 없음. 유아어·학술 문어체·반응 강요·뻔한 연결어 등 금지 표현 없음

관련 파일:
- /home/lee/project/.claude/shortform/episodes/general-ep01-untitled/02-script-v1.md
- /home/lee/project/.claude/shortform/episodes/general-ep01-untitled/01-research.md
- /home/lee/project/.claude/shortform/profiles/general.md
